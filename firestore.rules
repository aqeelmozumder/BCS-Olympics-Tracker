rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /countries/{country} {
      // Allow anyone to read the scoreboard (Public View)
      allow read: if true;
      
      // Only allow logged-in staff to edit/add/delete (Admin View)
      allow write: if request.auth != null;
    }
  }
}